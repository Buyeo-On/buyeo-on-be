---
id: UC-24
title: 여행 포인트 정산
owner: point
participants: [trip, badge, notification]
policies: [authorization, idempotency, date-time, concurrency, transactions]
api: ["PUT /trips/{tripId}/settlement"]
adrs: [ADR-003]
---

# 여행 포인트 정산

## 목적

사용자가 여행에서 남은 포인트의 처리 방법을 선택한다.

## 사전 조건

- 사용자가 로그인 상태다.
- 여행 세션이 종료된 상태다.

## 기본 흐름

1. 앱이 이번 여행에서 적립하고 아직 정산하지 않은 포인트와 사용 가능 기간을 표시한다. 다른 여행에서 이월한 포인트는 정산 대상에서 제외한다.
2. 사용자가 `부여에 포인트 남기고 가기` 또는 `다음에 이어서 쓰기`를 선택한다.
3. 포인트를 남기면 앱이 골목상권 기여 결과를 표시한다.
4. 이어서 쓰기를 선택하면 앱이 포인트를 10일 동안 유지한다.
5. 양수 포인트를 부여에 남겨 배지를 새로 획득하면 앱이 정산 response의 배지 ID, 이름, 이미지, 표시 조건과 획득 시각을 보여준다.
6. 앱이 정산 결과를 표시하고 발자취 조회로 이동한다.

## 예외 흐름

- 남은 포인트가 없으면 정산을 건너뛴다.
- 이미 정산된 여행을 다른 멱등성 키로 다시 정산하면 `409` 오류를 안내한다.
- 처리에 실패하면 기존 포인트를 유지하고 오류를 안내한다.
